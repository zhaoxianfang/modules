<?php

use Illuminate\Support\Facades\Route;
use {{NAMESPACE}}\{{NAME}}\Http\Controllers\Web;

/*
|--------------------------------------------------------------------------
| {{NAME}} 模块的 Web 路由
|--------------------------------------------------------------------------
|
| 在这里注册 {{NAME}} 模块的 Web 路由
|
| 路由配置：
|   - 已经自动应用 modules.middleware_groups 配置的 web 对应项的中间件组
|   - 路由前缀: {{ROUTE_PREFIX_VALUE}}
|   - 路由名称前缀: {{ROUTE_NAME_PREFIX_VALUE}}
*/

Route::middleware(['web'])->prefix('{{ROUTE_PREFIX_VALUE}}')->name('{{ROUTE_NAME_PREFIX_VALUE}}')->group(function () {
    /*
    |--------------------------------------------------------------------------
    | {{NAME}} 首页路由
    |--------------------------------------------------------------------------
    */
    Route::get('/', [Web\{{NAME}}Controller::class, 'index'])->name('index');

    /*
    |--------------------------------------------------------------------------
    | 完整资源路由（推荐）
    |--------------------------------------------------------------------------
    | 自动生成所有 RESTful 路由：
    | index, create, store, show, edit, update, destroy
    */
    // Route::resource('{{LOWER_NAME}}', Web\{{NAME}}Controller::class)->names('{{LOWER_NAME}}');

});



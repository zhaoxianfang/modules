<?php

return [
    /*
    |--------------------------------------------------------------------------
    | 模块启用状态
    |--------------------------------------------------------------------------
    | 控制模块是否启用
    | 设置为 false 可禁用模块，禁用后模块将不会加载
    |
    */
    'enable' => true,

    /*
    |--------------------------------------------------------------------------
    | 模块配置
    |--------------------------------------------------------------------------
    | 在这里添加模块的自定义配置
    | 可以通过 config('{{LOWER_NAME}}.config.key') 访问
    |
    */
    'config' => [
        // 示例配置
        'option' => 'value',
    ],
];
